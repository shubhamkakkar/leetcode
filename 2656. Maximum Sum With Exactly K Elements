/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
var maximizeSum = function(nums, k) {
    let max = Math.max(...nums);
    let sum = max;
    k = k -1;
    while (k != 0) {
        max = max + 1
        sum += max;
        k -= 1
    };
    return sum;
};
